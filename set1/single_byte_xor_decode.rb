describe 'decoding a hex encoded, single byte XORed string' do
  # decode the cipher hex value string to byte arrays
  # iterate through all possible values in a byte
  # for each possible value from 0 to 255, XOR each byte in the ciphertext
  # run a frequency analysis of the resulting candidate plaintext
  # capture the hash generated
end
